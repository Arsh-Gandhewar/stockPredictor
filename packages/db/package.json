{
  "name": "db",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "index.ts",
  "scripts": {
    "generate": "prisma generate",
    "build": "npx tsc --module commonjs --moduleResolution node --target ES2023 --esModuleInterop --outDir dist --declaration index.ts --skipLibCheck"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "prisma": "^6.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
